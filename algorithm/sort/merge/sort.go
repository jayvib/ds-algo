package merge

func SortInt(data []int) {
	mergeSort(data)
}

func mergeSort(data []int) {
	if len(data) > 2 {
		// Divide the array into two arrays
		// by calculating the middle index

		// Recursive merge sort for left
		// Recursive merge sort for right

		// Compare and Insert the lesser value
		// data.
	}
}
